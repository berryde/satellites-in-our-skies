const config = {
	content: ['./src/**/*.{html,js,svelte,ts}'],

	theme: {
		fontFamily: {
			sans: ["barlow", "ui-sans-serif", "system-ui"]
		},
		extend: {}
	},

	plugins: []
};

module.exports = config;
